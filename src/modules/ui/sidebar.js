export default function Sidebar() {
    // Creating the sidebar
    const sidebar = document.createElement("div");
    sidebar.classList.add("sidebar");

    // Add workspace code here (workspace code shoud be in a seperate component)
    return sidebar;
}